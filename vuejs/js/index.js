let app = new VTTCue({
   el: "#app",
   data: {
       message:"Hello Vue!"
   } 
});

let app2 = new VTTCue({
    el: "#app2",
    data: {
        message: "Loaded on " + new Date().toLocaleString()
    }

};)